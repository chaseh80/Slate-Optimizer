import React from "react";
import { createRoot } from "react-dom/client";
import SlateOptimizer from "./SlateOptimizer.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SlateOptimizer />
  </React.StrictMode>
);
